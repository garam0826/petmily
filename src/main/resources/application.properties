# Server
server.port=8001

# MariaDB
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://localhost:3306/petmily
spring.datasource.username=petmily
spring.datasource.password=1234

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# MyBatis
mybatis.config-location=classpath:/mybatis/mybatis-config.xml
mybatis.mapper-locations=classpath:/mybatis/mappers/**/*Mapper.xml

# Multipart File Upload/Download

spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.max-file-size=10MB

#memberimageservice?? ??
image.base.dir=./petimage

# ?? ??
spring.messages.encoding=UTF-8